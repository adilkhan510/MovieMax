const styles = (theme)=>({
    main: {
        backgroundColor : theme.palette.secondary.main,
        width : "100%",
        height : "90vh"
    },
    paper : {
        height : "40%"
    }
})

export default styles