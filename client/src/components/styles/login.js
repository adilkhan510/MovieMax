const styles = (theme)=>({
    paper : {
        width : "50vw",
        height : "50vh",
        transform : "translate(50%, 50%)",
        backgroundColor : theme.palette.secondary.main
    }
})

export default styles

