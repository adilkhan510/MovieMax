const styles = (theme)=>({
    root : {
        display : "flex",
        flexDirection : "row",
        justifyContent : "center",
        width : "100vw",
        backgroundColor : theme.palette.secondary.main,
    },
})


export default styles