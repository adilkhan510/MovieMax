import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
    palette : {
        primary : {
            main : "#e63946"
        },
        secondary : {
            main : "#f1faee"
        },
        warning : {
            main : "#a8dadc"
        },
        success : {
            main : "#457b9d"
        }
    }
})

export default theme